# Test Script

```bash
grep -q "myevsals v2" index.js && echo "PASS" || echo "FAIL"
grep -q "text/plain" index.js && echo "PASS" || echo "FAIL"
grep -q "3000" index.js && echo "PASS" || echo "FAIL"
```
