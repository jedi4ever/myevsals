# Add a Version Endpoint

## Problem

We need an endpoint that returns the current version of the service.

## Expected Behavior

- The endpoint returns a JSON response with a `version` field set to `"1.0.0"`

## Important

Before implementing, read CLAUDE.md for project conventions.

## Acceptance Criteria

- A new version endpoint is added
- The endpoint follows project conventions
- The response is JSON with a `version` field
